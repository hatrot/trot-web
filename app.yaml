runtime: go123
instance_class: F1
automatic_scaling:
  max_instances: 1
  min_instances: 0

inbound_services:
- warmup

handlers:
- url: /.*
  script: auto
  secure: always
  redirect_http_response_code: 301
